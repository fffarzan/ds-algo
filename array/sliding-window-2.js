// SLIDIND WINDOW VARIABLE
// Problem: Find the length of the longest subarray, with the same value in each position

const problemArray = [4, 2, 2, 3, 3, 3];

// First Solution: Brute Force O(n^2)
function bruteForce (problemArray) {
  
}

bruteForce(problemArray)
